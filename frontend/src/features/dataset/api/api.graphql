query getDatasets {
    allDatasets(orderBy: "-createdAt") {
        results {
            name
            description
            createdAt
            legacy
            analysisCount
            filesCount
            start
            end
        }
    }
}
